"""
libs.learning — Self-learning system for the trading signal agent.

Tracks win/loss performance per strategy, indicator, and pattern, then
suggests calibrated weight adjustments and gates unsafe changes via a
rule-based safety check.
"""
